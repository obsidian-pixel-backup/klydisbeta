<UserControl x:Class="Klydis.App.Controls.ColorPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <!-- Hex input row: live preview swatch + hex text box -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="PreviewSwatch" Width="30" Height="30" CornerRadius="6"
                    BorderBrush="{DynamicResource BorderMidBrush}" BorderThickness="1"
                    VerticalAlignment="Center"/>
            <TextBox x:Name="HexBox" AutomationProperties.Name="Hex colour value" Grid.Column="1" Margin="10,0,0,0"
                     VerticalAlignment="Center" FontFamily="Consolas" FontSize="12.5"
                     Padding="8,5" CharacterCasing="Upper"
                     Background="{DynamicResource InputBackgroundBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1" VerticalContentAlignment="Center"/>
        </Grid>

        <!-- Preset swatches (populated in code-behind) -->
        <WrapPanel x:Name="PresetsPanel" Margin="0,0,0,10"/>

        <!-- RGB sliders -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Text="R" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
            <Slider x:Name="RedSlider" AutomationProperties.Name="Red" Grid.Row="0" Grid.Column="1" Minimum="0" Maximum="255"
                    VerticalAlignment="Center" Margin="8,2" SmallChange="1" LargeChange="16"/>
            <TextBlock x:Name="RedValue" Grid.Row="0" Grid.Column="2" FontFamily="Consolas" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextAlignment="Right"/>

            <TextBlock Grid.Row="1" Text="G" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
            <Slider x:Name="GreenSlider" AutomationProperties.Name="Green" Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="255"
                    VerticalAlignment="Center" Margin="8,2" SmallChange="1" LargeChange="16"/>
            <TextBlock x:Name="GreenValue" Grid.Row="1" Grid.Column="2" FontFamily="Consolas" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextAlignment="Right"/>

            <TextBlock Grid.Row="2" Text="B" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
            <Slider x:Name="BlueSlider" AutomationProperties.Name="Blue" Grid.Row="2" Grid.Column="1" Minimum="0" Maximum="255"
                    VerticalAlignment="Center" Margin="8,2" SmallChange="1" LargeChange="16"/>
            <TextBlock x:Name="BlueValue" Grid.Row="2" Grid.Column="2" FontFamily="Consolas" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextAlignment="Right"/>
        </Grid>
    </StackPanel>
</UserControl>
